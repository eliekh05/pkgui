# ─── Cloudflare Pages project ─────────────────────────────────────────────────
# This config is for `wrangler pages dev` local preview only.
# CI/CD deploys via: wrangler pages deploy dist/ --project-name=pkgui
#
# To create the project the first time:
#   wrangler pages project create pkgui

name = "pkgui"
compatibility_date = "2024-09-23"

[dev]
port = 8788
